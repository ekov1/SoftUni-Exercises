//numbers = [];
function multiplay2Numbers(numbers) {
    let number1 = Number(numbers[0]),
        number2 = Number(numbers[1]),
        result = number1 * number2;
    return result;
}

function multiplay2Numbers(numbers) {
        result = numbers[0] * numbers[1];
    return result;
}